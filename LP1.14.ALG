algoritimo "14"
var
	t : real
  v : real
inicio
	escreval("digite o tamanho do arquivo em bits")
 		leia(t)
  escreval("digite a velocidade da conexao em bits por segundo [bps]: ")
  	leia(v)
  escreval("o download sera concluido em aproximadamente: ", t / v, " segundos")
fimalgoritimo
